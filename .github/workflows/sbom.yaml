name: SBOM Upload

on:
  push:
    branches: [main, dev]

jobs:
  sbom:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  #v6.0.2
        with:
          persist-credentials: false

      - uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57  #v8.0.0
        with:
          enable-cache: false

      - name: Get project version
        id: version
        run: echo "value=$(uv version | awk '{print $2}')" >> "$GITHUB_OUTPUT"

      - name: Generate SBOM
        run: |
          uv sync --frozen --no-dev
          uvx --from cyclonedx-bom cyclonedx-py environment \
              --pyproject pyproject.toml \
              --mc-type application \
              -o sbom.cdx.json \
              .venv/bin/python

      - name: Authenticate to GCP
        id: auth
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093  #v3.0.0
        with:
          workload_identity_provider: "${{ secrets.DTRACK_WIF }}"
          service_account: "${{ secrets.DTRACK_WIF_SA }}"
          token_format: 'id_token'
          id_token_audience: "${{ secrets.DTRACK_WIF_AUDIENCE }}"
          id_token_include_email: true

      - name: Upload SBOM to Dependency-Track
        env:
          TOKEN: ${{ steps.auth.outputs.id_token }}
          PROJECT_NAME: ${{ github.event.repository.name }}
          PROJECT_VERSION: ${{ steps.version.outputs.value }}
        run: |
            jq -n \
              --arg name "$PROJECT_NAME" \
              --arg version "$PROJECT_VERSION" \
              --arg bom "$(base64 -w0 sbom.cdx.json)" \
              --arg ref "$GITHUB_REF_NAME" \
              '{
                projectName: $name,
                projectVersion: $version,
                autoCreate: true,
                bom: $bom,
                projectTags: [
                  {name: "python"},
                  {name: $ref}
                ]
              }' | \
            curl -sf -X PUT "https://dtrack.popgen.rocks/api/v1/bom" \
              -H "Authorization: Bearer $TOKEN" \
              -H "X-Api-Key: ${{ secrets.DTRACK_API_KEY }}" \
              -H "Content-Type: application/json" \
              -d @-
